import { Router } from 'express';

const router = Router();

// FEATURE INTEGRATION POINT
// Import and use feature-specific external routers here.
// Example: router.use('/auth', authRoutes);

export default router;
