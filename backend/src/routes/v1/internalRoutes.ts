import { Router } from 'express';

const router = Router();

// FEATURE INTEGRATION POINT
// Import and use feature-specific internal routers here.
// Example: router.use('/tasks', taskRoutes);

export default router;
